# Production build settings. These two values are PUBLIC by design: they ship
# inside the browser bundle no matter where they are set, and Row Level
# Security on every table is what protects each user's data. Committing them
# here makes every Vercel build deterministic, independent of dashboard
# settings. Variables set in the Vercel dashboard still take priority.
VITE_SUPABASE_URL=https://zuivkqjyierbcaqhmuhd.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_6tm7cMe8g95C_GcRWV8uBw_FGtoYbwo
